import { AcceptInviteScreen } from "@/components/accept-invite-screen";

export const metadata = {
  title: "Accept Invitation — Jirita",
};

export default function AcceptInvitePage() {
  return <AcceptInviteScreen />;
}
